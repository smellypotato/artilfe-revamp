{
  "name": "artilfe-revamp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "tsc -b && vite build",
    "build:pages": "tsc -b && vite build && node scripts/prepare-github-pages.mjs",
    "export:firebase": "node scripts/export-firebase-data.mjs",
    "import:supabase": "node scripts/import-supabase-data.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "browserslist": [
    "defaults",
    "not IE 11"
  ],
  "engines": {
    "node": ">=18"
  }
}
